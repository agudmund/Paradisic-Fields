name: Cozy CI 🌱

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # If you ever add requirements.txt, it will install here automatically
        # pip install -r requirements.txt

    - name: Install global Cozy package (editable)
      run: |
        # This assumes the cozy package lives next to the repo in your local setup
        # For CI we skip it for now — the app still runs fine
        echo "Cozy package would be installed here in a full setup 🌿"

    - name: Check syntax & import
      run: |
        python -m pycompile main.py
        python -c "import sys; sys.path.insert(0, '.'); import cozy; print('✅ Cozy imports cleanly!')"